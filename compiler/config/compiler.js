
var compilerOpt = {

    /* Activate Nunjucks static compilation ? */
    useNunjucks : true,

    /* Use Ruby Sass instead of libsass for Compilation ? */
    useRubySass : true

};

exports.compilerOpt = compilerOpt;
